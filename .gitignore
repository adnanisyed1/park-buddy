node_modules
.next
out
.env*.local
# any .env variant (incl. typos like .env.loccd) — never let a secrets file into git
.env*
!.env.example
.DS_Store
*.log
.vercel

.netlify
.claude/settings.local.json

# byway ingest geocode cache (build-time)
scripts/.cache/

# build-time geometry cache — polygons are fetched, used, and thrown away
scripts/.cache/
scripts/.lake-cache/
scripts/.lake-crawl.log
scripts/.overview-cache/
