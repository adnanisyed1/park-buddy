import TripBook from "./TripBook";

export const metadata = {
  title: "Your Trip Book — Park Buddy",
  description: "A preview of your trip turned into a keepsake book: cover, a page for every stop with your photos and the story you write along the way.",
};

export default function TripBookPage() {
  return <TripBook />;
}
