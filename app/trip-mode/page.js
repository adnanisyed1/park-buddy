import TripMode from "./TripMode";

export const metadata = {
  title: "Trip Mode — Park Buddy",
  description: "Live on-trip mode: track your route with permission, get photo prompts at scenic stops, a smart packing checklist, and today's safety & conditions for where you're headed.",
};

export default function TripModePage() {
  return <TripMode />;
}
