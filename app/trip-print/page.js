import TripPrint from "./TripPrint";

export const metadata = {
  title: "Your trip itinerary — Park Buddy",
  description: "A print-ready national-parks road-trip itinerary: stops, day-by-day plan, budget and a packing checklist.",
};

export default function TripPrintPage() {
  return <TripPrint />;
}
